<h1>Articles</h1>

<p> O site possui <%= Article.public_count %> artigos e contando. </p>

<ul>
    <% @articles.each do |article| %>
    <li>
        <%= link_to article.title,article %>
    </li>
    <% end %>
</ul>

<%= link_to "New Article", new_article_path %>

<%= link_to "Back", root_path %>